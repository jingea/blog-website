# java内存模型

### 缓存一致性
基于高速缓存的存储交互很好地解决了处理器和内存的速度矛盾,但是也引入了新的问题:缓存一致性.

在多核处理器系统中,每个处理器都有自己的告诉缓存,而他们又共享同一内存,当多个处理器的运算任务
都涉及同一主内存区域时,将可能导致各自的缓存不一致情况.

为了解决缓存一致性问题,各个处理器访问内存时都遵循一些协议,如`MSI`, `MOSI`, `Synapse`等

### 代码乱序执行
* 为了使得处理器内部的运算单元内尽量被充分利用,处理器可能会对输入代码进行乱序执行优化
* 另外JVM的即时编译器类似的指令重排序优化

